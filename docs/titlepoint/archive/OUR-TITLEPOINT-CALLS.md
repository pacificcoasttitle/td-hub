# ARCHIVED: superseded by `TITLEPOINT_IMPLEMENTATION_SOURCE_OF_TRUTH.md` after live validation and legacy evidence reconciliation.

## Why this file was archived

This document captured an investigative snapshot while the integration was still unresolved.

It is not safe as an active source of truth anymore because it contains now-disproven statements, including:

- `all calls currently return HTTP 403`
- unresolved transport guesses treated as current state
- outdated credential value without the required trailing `#`
- pre-fix request/response assumptions that were later replaced by live vendor validation

## What replaced it

Use these instead:

- `docs/titlepoint/TITLEPOINT_IMPLEMENTATION_SOURCE_OF_TRUTH.md`
- `docs/titlepoint/TITLEPOINT_DEBUG_TIMELINE_2026-03.md`
- `docs/titlepoint/TITLEPOINT_LEGACY_EVIDENCE_PACK.md`

## Historical note

This archived filename is preserved so future developers can see that the repo once carried a misleading call-by-call snapshot during the debugging phase.
